import { CustomstylingDirective } from './customstyling.directive';

describe('CustomstylingDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomstylingDirective();
    expect(directive).toBeTruthy();
  });
});
